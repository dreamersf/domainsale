
export interface DomainMetadata {
  name: string;
  extension: string;
  category: string[];
  length: number;
  description: string;
}

export interface UseCase {
  title: string;
  description: string;
  icon: string;
}
